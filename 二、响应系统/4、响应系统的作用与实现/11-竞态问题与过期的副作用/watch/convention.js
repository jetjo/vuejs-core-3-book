const FLUSH_TYPE = {
  // pre: Symbol('FLUSH_TYPE.pre'),
  // sync: Symbol('FLUSH_TYPE.sync'),
  // post: Symbol('FLUSH_TYPE.post')
  pre: 'FLUSH_TYPE.pre',
  sync: 'FLUSH_TYPE.sync',
  post: 'FLUSH_TYPE.post'
}

export { FLUSH_TYPE }
